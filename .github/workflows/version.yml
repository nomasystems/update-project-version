name: version

run-name: Update version

on:
  push:
    branches:
      - '**'

jobs:
  update_version:
    runs-on: ubuntu-latest
    name: Update version
    steps:
      - id: new_version
        uses: nomasystems/update-project-version@latest
        with:
          committer-email: "87307170+AidaAP@users.noreply.github.com"
          committer-username: "AidaAP"
          latest-tag: "true"
          tag-prefix: "v"
